import 'package:flutter/material.dart';

class MySearchSuggestions extends StatelessWidget {
  const MySearchSuggestions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Haaaaa",style: TextStyle(color: Colors.white),),
    );
  }
}